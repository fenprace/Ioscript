# Ioscript

```
"Hello, world!" print

Element := Object clone
# the prototype of Element is the prototype of Object

mg := Element clone
# mg is an instant of Element, so the prototype of mg is Element
```